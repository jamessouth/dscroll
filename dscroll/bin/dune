(executable
 (libraries
  core
  ; core_bench
  core_unix.command_unix
  dscroll)
 (modes native)
 (name main)
 ; preserves asm files
 ; (ocamlopt_flags
 ;  (:standard -S))
 (ocamlopt_flags
  (:standard -g))
 (preprocess
  (pps ppx_jane))
 (public_name dscroll))
